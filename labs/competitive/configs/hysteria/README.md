# Hysteria Configs

Milestone A detects the `hysteria` binary and records skipped rows when it is unavailable.

Add concrete Hysteria 2 client/server configs here before enabling Hysteria benchmark rows.
