# Docker

Milestone A is native-first for this repository and the supplied VPS hosts. This directory is reserved for future containerized reproducibility artifacts, but the current `competitive-*` targets do not use Docker.
