# Selects the stable Rust release used by local development, CI, and container
# builds. Cargo.toml records the minimum supported Rust version separately.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
